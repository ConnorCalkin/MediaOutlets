version: "3.9"

services:
  chroma:
    image: chromadb/chroma
    container_name: chroma
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/data
    command: run --host 0.0.0.0 --port 8000 --path /data

volumes:
  chroma_data: