# Named Entity Recognition Pipeline

Extracts people and organisations from article text using spaCy.

## Prerequisites
- Docker installed

## Setup & Run
```bash
docker build -t ner-pipeline .
docker run ner-pipeline
```